package com.bigdata2026.streaming

object Main {
  def main(args: Array[String]): Unit = {
    println(
      "[streaming] Spark Structured Streaming skeleton. " +
        "Build SparkSession, readStream from Kafka topic, writeStream to HBase."
    )
  }
}
